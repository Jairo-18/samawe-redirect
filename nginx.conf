server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Redirect everything to the new domain
    # This is a server-side redirect which is faster than the Angular/meta refreshes
    return 301 https://ecohotelsamawe.com$request_uri;
}
